<resources>
    <style name="CustomDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!-- Time Picker 테마 -->
    <style name="InfoTimePickerTheme" parent="android:Widget.Material.Light.TimePicker">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/smupool_gray</item>

        <!-- NumberPicker divider color -->
        <item name="colorControlNormal">@color/smupool_blue</item>
    </style>
</resources>
